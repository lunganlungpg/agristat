% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posthoc.R
\name{test_contrasts}
\alias{test_contrasts}
\title{Orthogonal Contrast Testing}
\usage{
test_contrasts(model, contrast_matrix, validate = TRUE, tol = 1e-08)
}
\arguments{
\item{model}{A fitted \code{"aov"} object.}

\item{contrast_matrix}{A numeric matrix where each \strong{row} is one contrast.
Columns must correspond to treatment levels in the same order as
\code{levels(treatment_factor)}. Row names are used as contrast labels.}

\item{validate}{Logical; whether to validate orthogonality (default \code{TRUE}).}

\item{tol}{Tolerance for checking contrast conditions (default \code{1e-8}).}
}
\value{
An S3 object of class \code{contrast_result} containing:
\describe{
\item{\code{contrasts}}{Data frame: contrast name, SS, df, F, p-value.}
\item{\code{sum_of_squares_contrasts}}{Named numeric vector of contrast SS.}
\item{\code{anova_table}}{Standard ANOVA table from the fitted model.}
\item{\code{orthogonal}}{Logical indicating whether contrasts are orthogonal.}
\item{\code{interpretation}}{Character summary.}
}
}
\description{
Tests user-defined orthogonal contrasts against the ANOVA error term,
partitioning the treatment sum of squares into single-degree-of-freedom
components.
}
\details{
For a contrast \eqn{\mathbf{c}^\top \boldsymbol{\mu}} with coefficients
\eqn{c_1, \ldots, c_k} (satisfying \eqn{\sum c_i = 0}), the contrast sum
of squares is:
\deqn{SS_c = \frac{n (\sum c_i \bar{y}_i)^2}{\sum c_i^2}}
(balanced design with \eqn{n} reps per treatment).

Two contrasts \eqn{\mathbf{c}} and \eqn{\mathbf{d}} are orthogonal when
\eqn{\sum c_i d_i / n_i = 0}.
}
\examples{
set.seed(7)
dat <- data.frame(
  y = c(rnorm(4, 10), rnorm(4, 12), rnorm(4, 14), rnorm(4, 11)),
  trt = factor(rep(c("A", "B", "C", "D"), each = 4))
)
fit <- aov(y ~ trt, data = dat)
cmat <- rbind(
  "A+B vs C+D"  = c( 1,  1, -1, -1),
  "A vs B"      = c( 1, -1,  0,  0),
  "C vs D"      = c( 0,  0,  1, -1)
)
res <- test_contrasts(fit, cmat)
print(res)

}
\seealso{
\code{\link[=dmrt]{dmrt()}}, \code{\link[=tukey_hsd]{tukey_hsd()}}, \code{\link[=mean_separation]{mean_separation()}}
}
