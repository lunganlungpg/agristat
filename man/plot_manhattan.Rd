% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genetics.R
\name{plot_manhattan}
\alias{plot_manhattan}
\title{Manhattan Plot for GWAS Results}
\usage{
plot_manhattan(
  x,
  threshold = NULL,
  suggestive = NULL,
  top_n = 5L,
  title = "Manhattan Plot"
)
}
\arguments{
\item{x}{A \code{gwas_result} object returned by \code{\link[=run_basic_gwas]{run_basic_gwas()}}.}

\item{threshold}{Numeric.  -log10(p) significance threshold line
(default \code{NULL} uses the Bonferroni threshold stored in \code{x}).}

\item{suggestive}{Numeric or \code{NULL}.  Suggestive threshold (default
\code{NULL} = none).}

\item{top_n}{Integer.  Number of top SNPs to annotate with their name
(default \code{5L}).}

\item{title}{Character.  Plot title (default \code{"Manhattan Plot"}).}
}
\value{
A \code{ggplot} object.
}
\description{
Plots SNP association p-values across the genome, with chromosomes
shown in alternating colours and a Bonferroni significance threshold
line.
}
\examples{
set.seed(6)
n <- 100L; m <- 50L
pheno <- rnorm(n)
gmat  <- matrix(sample(0:2, n * m, replace = TRUE),
                nrow = n,
                dimnames = list(NULL, paste0("SNP", seq_len(m))))
pos <- data.frame(marker     = paste0("SNP", seq_len(m)),
                  chromosome = rep(1:5, each = 10L),
                  position   = rep(seq(1, 100, 10), 5L))
g <- run_basic_gwas(pheno, gmat, pos)
p <- plot_manhattan(g)
# print(p)

}
\seealso{
\code{\link[=run_basic_gwas]{run_basic_gwas()}}, \code{\link[=plot_qq_gwas]{plot_qq_gwas()}}
}
