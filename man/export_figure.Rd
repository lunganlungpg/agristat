% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thesis-export.R
\name{export_figure}
\alias{export_figure}
\title{Export a Plot to a File}
\usage{
export_figure(
  plot_obj = NULL,
  file,
  format = "png",
  width = 7,
  height = 5,
  dpi = 300,
  title = NULL,
  experiment_id = NULL
)
}
\arguments{
\item{plot_obj}{A \code{ggplot} object, or \code{NULL} to capture the current
base-graphics device.}

\item{file}{Character.  Output file path (including extension or just stem
— the correct extension is appended automatically if missing).}

\item{format}{Character.  One of \code{"png"} (default), \code{"pdf"}, \code{"svg"},
or \code{"eps"}.}

\item{width}{Numeric.  Figure width in inches (default \code{7}).}

\item{height}{Numeric.  Figure height in inches (default \code{5}).}

\item{dpi}{Integer.  Dots per inch for raster formats (default \code{300}).}

\item{title}{Character or \code{NULL}.  Optional title prepended via
\code{ggplot2::ggtitle()} before saving (ggplot2 objects only).}

\item{experiment_id}{Character or \code{NULL}.  Optional identifier appended to
the figure metadata comment.}
}
\value{
Invisibly, a named list with elements:
\describe{
\item{\code{file_path}}{Absolute path to the saved file.}
\item{\code{size_kb}}{File size in kilobytes.}
\item{\code{resolution}}{DPI value used.}
\item{\code{format}}{Format string used.}
}
}
\description{
Saves a ggplot2 object (or the current base-graphics device) to a file in
one of several publication-quality formats.
}
\examples{
\dontrun{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg)) + geom_point()
info <- export_figure(p, file = "scatter.png", format = "png", dpi = 150)
info$size_kb
}

}
\seealso{
\code{\link[=compile_report]{compile_report()}}, \code{\link[=generate_appendix]{generate_appendix()}}
}
