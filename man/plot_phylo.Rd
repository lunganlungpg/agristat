% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phylogenetics.R
\name{plot_phylo}
\alias{plot_phylo}
\title{Plot a Phylogenetic Tree}
\usage{
plot_phylo(
  tree,
  main = "Phylogenetic Tree",
  support_threshold = 70,
  tip_labels = TRUE,
  ...
)
}
\arguments{
\item{tree}{A \code{phylo_tree_ml}, \code{phylo_tree_nj}, \code{bootstrap_result}, or
\code{ape::phylo} object.}

\item{main}{Character. Plot title. Default \code{"Phylogenetic Tree"}.}

\item{support_threshold}{Numeric. Bootstrap support threshold for
highlighting clades (default 70).}

\item{tip_labels}{Logical. Show tip labels? Default \code{TRUE}.}

\item{...}{Extra arguments forwarded to \code{ape::plot.phylo()}.}
}
\value{
ggplot2 object (ggtree) or \code{NULL} invisibly (base graphics).
}
\description{
Produces a phylogenetic tree plot. Uses \strong{ggtree} when available;
otherwise uses \code{ape::plot.phylo()} base graphics.
}
\examples{
fasta_lines <- c(
  ">Seq1", "ATGCATGCATGCATGCATGC",
  ">Seq2", "ATGCTTGCATGCATGCTTGC",
  ">Seq3", "ATGCATGCTTGCATGCATGC",
  ">Seq4", "ATGCATGCATGCTTGCATGC"
)
tmp <- tempfile(fileext = ".fasta")
writeLines(fasta_lines, tmp)
seqs <- read_fasta(tmp)
aln  <- align_sequences(seqs)
tree <- build_tree_nj(aln)
plot_phylo(tree)

}
