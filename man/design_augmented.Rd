% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genetics.R
\name{design_augmented}
\alias{design_augmented}
\title{Generate an Augmented Field Design}
\usage{
design_augmented(
  test_genotypes,
  check_genotypes,
  replicates_checks = 2L,
  seed = NULL
)
}
\arguments{
\item{test_genotypes}{Character vector.  Names of unreplicated test lines.}

\item{check_genotypes}{Character vector.  Names of replicated check
genotypes (>= 1).}

\item{replicates_checks}{Integer.  Number of complete blocks.  Each
check genotype appears once per block.}

\item{seed}{Integer or \code{NULL}.  Random seed for reproducibility.}
}
\value{
A \code{\link[=data.frame]{data.frame()}} with columns:
\describe{
\item{block}{Block number (integer).}
\item{plot_id}{Sequential plot identifier (integer).}
\item{genotype}{Genotype name (character).}
\item{type}{\code{"check"} or \code{"test"}.}
}
}
\description{
Creates an augmented randomised complete block design suitable for
evaluating a large number of unreplicated test lines alongside
replicated check genotypes.
}
\details{
In an augmented design:
\itemize{
\item Each \strong{check} genotype appears in every block (replicated
\code{replicates_checks} times, i.e., once per block when
\code{replicates_checks} = number of blocks).
\item Each \strong{test} genotype appears in exactly one block (unreplicated).
}
}
\examples{
tests  <- paste0("T", 1:12)
checks <- c("Check1", "Check2", "Check3")
d <- design_augmented(tests, checks, replicates_checks = 4L, seed = 7L)
table(d$type)

}
\references{
Federer, W. T. (1956). Augmented (or Hoonuiaku) designs.
\emph{Hawaiian Planters' Record}, 55, 191--208.
}
\seealso{
\code{\link[=design_alpha_lattice]{design_alpha_lattice()}}, \code{\link[=design_incomplete_block]{design_incomplete_block()}}
}
