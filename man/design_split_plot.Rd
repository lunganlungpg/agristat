% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field-layouts.R
\name{design_split_plot}
\alias{design_split_plot}
\title{Generate a Split-Plot Design}
\usage{
design_split_plot(whole_plot_factor, sub_plot_factor, blocks, seed = NULL)
}
\arguments{
\item{whole_plot_factor}{Character vector of whole-plot factor levels.}

\item{sub_plot_factor}{Character vector of sub-plot factor levels.}

\item{blocks}{Positive integer.  Number of blocks (replications of the
whole-plot factor).}

\item{seed}{Integer or \code{NULL}.  Random seed.}
}
\value{
An S3 object of class \code{c("split_plot_design", "field_design")} with
a \code{layout} data frame and an \code{error_structure} list containing
\code{whole_plot_error} and \code{sub_plot_error} degrees of freedom.
}
\description{
Creates a split-plot experimental design where one factor (the \emph{whole-plot}
factor) is applied to large experimental units, and a second factor (the
\emph{sub-plot} factor) is applied within each whole-plot.
}
\details{
The split-plot model contains two error strata:
\deqn{y_{ijk} = \mu + \rho_k + \alpha_i + \delta_{ik} + \beta_j +
  (\alpha\beta)_{ij} + \varepsilon_{ijk}}
where \eqn{\delta_{ik}} is the whole-plot error and
\eqn{\varepsilon_{ijk}} is the sub-plot error.
}
\examples{
d <- design_split_plot(
  whole_plot_factor = c("Irrigation-On", "Irrigation-Off"),
  sub_plot_factor   = c("V1", "V2", "V3"),
  blocks            = 4L,
  seed              = 99L
)
print(d)

}
\seealso{
\code{\link[=design_split_split_plot]{design_split_split_plot()}}, \code{\link[=design_strip_plot]{design_strip_plot()}},
\code{\link[=analyze_design]{analyze_design()}}
}
