% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{validate_contrasts}
\alias{validate_contrasts}
\title{Validate Orthogonality of a Contrast Matrix}
\usage{
validate_contrasts(contrast_matrix, tol = 1e-08)
}
\arguments{
\item{contrast_matrix}{A numeric matrix with one contrast per row.}

\item{tol}{Numerical tolerance for zero-checking (default \code{1e-8}).}
}
\value{
A single logical value: \code{TRUE} if all conditions are met, \code{FALSE}
otherwise. Diagnostic messages are printed via \code{message()}.
}
\description{
Checks whether the rows of a contrast matrix satisfy the two conditions for
orthogonality: (1) each row sums to zero, and (2) the inner product of any
two distinct rows is zero.
}
\examples{
cmat <- rbind(
  c( 1,  1, -1, -1),
  c( 1, -1,  0,  0),
  c( 0,  0,  1, -1)
)
validate_contrasts(cmat)

# Non-orthogonal example
bad <- rbind(c(1, -1, 0), c(1, 0, -1))
validate_contrasts(bad)

}
