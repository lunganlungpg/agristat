% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field-layouts.R
\name{design_crd}
\alias{design_crd}
\title{Generate a Completely Randomised Design (CRD)}
\usage{
design_crd(treatments, replicates, seed = NULL)
}
\arguments{
\item{treatments}{Character or numeric vector of treatment labels.  Must
have at least 2 unique values.}

\item{replicates}{Positive integer.  Number of replicates (experimental
units) per treatment.}

\item{seed}{Integer or \code{NULL}.  Random seed for reproducibility.  Default
\code{NULL} uses the current RNG state.}
}
\value{
An S3 object of class \code{c("crd_design", "field_design")} with a
\code{layout} data frame containing columns:
\describe{
\item{\code{plot_id}}{Integer plot number (sequential).}
\item{\code{treatment}}{Treatment label.}
\item{\code{block}}{Always \code{NA} for CRD.}
\item{\code{row}}{Integer row position in the field.}
\item{\code{column}}{Integer column position in the field.}
}
}
\description{
Creates a randomised plot layout for a \strong{Completely Randomised Design}
(CRD) in which treatments are assigned to experimental units with no
blocking.  This is the simplest experimental design and assumes homogeneous
experimental material.
}
\details{
The statistical model for CRD is:
\deqn{y_{ij} = \mu + \tau_i + \varepsilon_{ij}}
where \eqn{\mu} is the overall mean, \eqn{\tau_i} is the \eqn{i}-th
treatment effect, and \eqn{\varepsilon_{ij} \sim N(0, \sigma^2)}.
}
\examples{
# Three treatments, four replicates each
d <- design_crd(
  treatments = c("Control", "Low-N", "High-N"),
  replicates = 4L,
  seed       = 42L
)
print(d)
head(d$layout)

}
\seealso{
\code{\link[=design_rcbd]{design_rcbd()}}, \code{\link[=design_factorial]{design_factorial()}}, \code{\link[=analyze_design]{analyze_design()}},
\code{\link[=plot_field_layout]{plot_field_layout()}}
}
