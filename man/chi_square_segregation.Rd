% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-ingestion.R
\name{chi_square_segregation}
\alias{chi_square_segregation}
\title{Chi-Square Test for Mendelian Segregation}
\usage{
chi_square_segregation(observed, expected_ratios, alpha = 0.05)
}
\arguments{
\item{observed}{Numeric vector of observed counts (integers \eqn{\geq 0}).}

\item{expected_ratios}{Numeric vector of the same length as \code{observed}
giving the expected ratio (e.g., \code{c(3, 1)} for a 3:1 ratio).}

\item{alpha}{Numeric significance level (default \code{0.05}).}
}
\value{
An S3 object of class \code{"chi_square_result"} with components:
\describe{
\item{\code{chi_sq_stat}}{Numeric. The \eqn{\chi^2} statistic.}
\item{\code{p_value}}{Numeric. P-value from the chi-square distribution.}
\item{\code{df}}{Integer. Degrees of freedom.}
\item{\code{observed}}{Numeric vector. Supplied observed counts.}
\item{\code{expected}}{Numeric vector. Expected counts derived from the
total and the ratios.}
\item{\code{ratio_string}}{Character. Human-readable ratio string
(e.g., \code{"3:1"}).}
\item{\code{interpretation}}{Character. Plain-language conclusion.}
\item{\code{goodness_of_fit}}{Logical. \code{TRUE} if the data are consistent
with the expected ratio at the given \code{alpha}.}
\item{\code{alpha}}{Numeric. Significance level used.}
}
}
\description{
Tests whether observed offspring counts are consistent with an expected
Mendelian segregation ratio using Pearson's chi-square goodness-of-fit test.
}
\details{
The chi-square statistic is:
\deqn{\chi^2 = \sum_{i} \frac{(O_i - E_i)^2}{E_i}}
with degrees of freedom \eqn{df = k - 1} where \eqn{k} is the number of
classes.
}
\examples{
# 3:1 ratio (75 dominant : 25 recessive)
result <- chi_square_segregation(
  observed        = c(75, 25),
  expected_ratios = c(3, 1)
)
print(result)

# 9:3:3:1 dihybrid ratio
result2 <- chi_square_segregation(
  observed        = c(90, 30, 30, 10),
  expected_ratios = c(9, 3, 3, 1)
)
print(result2)

}
\seealso{
\code{\link[=check_assumptions]{check_assumptions()}}
}
