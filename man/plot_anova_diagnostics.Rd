% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field-layouts.R
\name{plot_anova_diagnostics}
\alias{plot_anova_diagnostics}
\title{Plot ANOVA Diagnostic Plots}
\usage{
plot_anova_diagnostics(model, which = 1:4)
}
\arguments{
\item{model}{An \code{aov}, \code{lm}, or \code{lmerMod} object, or an \code{agristat_aov}
object.}

\item{which}{Integer vector (1–4) selecting which panels to draw.  Default
\code{1:4} (all four panels).}
}
\value{
Called for its side effect (base graphics plot).  Returns the model
invisibly.
}
\description{
Produces a 4-panel diagnostic plot for a fitted model object.  The panels
show:
\enumerate{
\item Residuals vs Fitted
\item Normal Q-Q plot of residuals
\item Scale-Location (sqrt(|residuals|) vs Fitted)
\item Residuals vs Leverage (Cook's distance contours)
}
}
\examples{
set.seed(1L)
d   <- design_crd(c("A", "B", "C"), replicates = 5L, seed = 1L)
df  <- d$layout
df$yield <- c(rnorm(5, 10), rnorm(5, 12), rnorm(5, 11))
res <- analyze_design(d, response = "yield", data = df)
plot_anova_diagnostics(res)

}
\seealso{
\code{\link[=analyze_design]{analyze_design()}}, \code{\link[=plot_field_layout]{plot_field_layout()}}
}
