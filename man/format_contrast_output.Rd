% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{format_contrast_output}
\alias{format_contrast_output}
\title{Format Contrast Output for Display}
\usage{
format_contrast_output(contrast_result, digits = 4, alpha = 0.05)
}
\arguments{
\item{contrast_result}{A \code{contrast_result} object from \code{\link[=test_contrasts]{test_contrasts()}}.}

\item{digits}{Integer; number of decimal places (default \code{4}).}

\item{alpha}{Significance level for marking significance (default \code{0.05}).}
}
\value{
A character string (invisibly) with a formatted table. The string
is also printed to the console.
}
\description{
Converts a \code{contrast_result} object into a neatly formatted character
string suitable for inclusion in reports or vignettes.
}
\examples{
set.seed(7)
dat <- data.frame(
  y = c(rnorm(4, 10), rnorm(4, 12), rnorm(4, 14), rnorm(4, 11)),
  trt = factor(rep(c("A", "B", "C", "D"), each = 4))
)
fit <- aov(y ~ trt, data = dat)
cmat <- rbind(
  "A+B vs C+D" = c( 1,  1, -1, -1),
  "A vs B"     = c( 1, -1,  0,  0),
  "C vs D"     = c( 0,  0,  1, -1)
)
res <- test_contrasts(fit, cmat)
format_contrast_output(res)

}
