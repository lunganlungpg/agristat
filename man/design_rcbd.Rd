% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field-layouts.R
\name{design_rcbd}
\alias{design_rcbd}
\title{Generate a Randomised Complete Block Design (RCBD)}
\usage{
design_rcbd(treatments, blocks, seed = NULL)
}
\arguments{
\item{treatments}{Character or numeric vector of treatment labels.}

\item{blocks}{Positive integer.  Number of complete blocks.}

\item{seed}{Integer or \code{NULL}.  Random seed.}
}
\value{
An S3 object of class \code{c("rcbd_design", "field_design")} with a
\code{layout} data frame with columns: \code{plot_id}, \code{treatment}, \code{block},
\code{row}, \code{column}.
}
\description{
Creates a randomised plot layout for a \strong{Randomised Complete Block Design}
(RCBD) in which each block contains exactly one complete set of treatments,
randomised independently within each block.  Blocks account for a known
source of field variation (e.g., soil gradient, topography).
}
\details{
The statistical model for RCBD is:
\deqn{y_{ij} = \mu + \tau_i + \beta_j + \varepsilon_{ij}}
where \eqn{\beta_j} is the \eqn{j}-th block effect.
}
\examples{
d <- design_rcbd(
  treatments = c("V1", "V2", "V3", "V4"),
  blocks     = 3L,
  seed       = 7L
)
print(d)
head(d$layout, 8)

}
\seealso{
\code{\link[=design_crd]{design_crd()}}, \code{\link[=analyze_design]{analyze_design()}}, \code{\link[=plot_field_layout]{plot_field_layout()}}
}
