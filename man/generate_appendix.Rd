% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thesis-export.R
\name{generate_appendix}
\alias{generate_appendix}
\title{Generate an Appendix Section}
\usage{
generate_appendix(data, design_obj = NULL, analysis_obj = NULL)
}
\arguments{
\item{data}{A \code{data.frame} of raw field/experimental data.}

\item{design_obj}{Optional. A field design object used to contextualise the
appendix header.}

\item{analysis_obj}{Optional. An \code{agristat_aov} object.  When provided,
residuals are extracted and tested for normality.}
}
\value{
An S3 object of class \code{"appendix"} with components:
\describe{
\item{\code{sections}}{Named list of Markdown-formatted character strings:
\code{data_summary}, \code{assumption_tests}, and \code{raw_data_table}.}
\item{\code{tables}}{Named list of data frames: \code{descriptive_stats} and
(if \code{analysis_obj} provided) \code{normality_test}.}
\item{\code{n_obs}}{Integer. Number of observations in \code{data}.}
\item{\code{n_missing}}{Integer. Total count of \code{NA} values in \code{data}.}
}
}
\description{
Produces a structured appendix comprising a data summary, optional
assumption tests (Shapiro-Wilk normality test on residuals), and a raw-data
table summary.  All text is formatted in Markdown.
}
\examples{
set.seed(42)
dat <- data.frame(
  yield = rnorm(20, 5, 1),
  trt   = rep(c("A", "B", "C", "D"), each = 5)
)
app <- generate_appendix(dat)
cat(app$sections$data_summary)

}
\seealso{
\code{\link[=compile_report]{compile_report()}}, \code{\link[=generate_methods_text]{generate_methods_text()}}
}
