% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posthoc.R
\name{plot_tukey}
\alias{plot_tukey}
\title{Tukey HSD Interval Plot}
\usage{
plot_tukey(
  x,
  title = "Tukey HSD Pairwise Comparisons",
  sig_color = "firebrick",
  ns_color = "grey50",
  ...
)
}
\arguments{
\item{x}{A \code{tukey_result} object from \code{\link[=tukey_hsd]{tukey_hsd()}}.}

\item{title}{Character; plot title. Default \code{"Tukey HSD Pairwise Comparisons"}.}

\item{sig_color}{Colour for significant comparisons (default \code{"firebrick"}).}

\item{ns_color}{Colour for non-significant comparisons (default \code{"grey50"}).}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{ggplot2} object.
}
\description{
Creates a ggplot2 interval plot showing all pairwise Tukey HSD confidence
intervals, with significant comparisons highlighted.
}
\examples{
set.seed(42)
dat <- data.frame(
  y = c(rnorm(5, 10), rnorm(5, 12), rnorm(5, 14)),
  trt = factor(rep(c("A", "B", "C"), each = 5))
)
fit <- aov(y ~ trt, data = dat)
tres <- tukey_hsd(fit)
p <- plot_tukey(tres)
print(p)

}
