% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genetics.R
\name{design_incomplete_block}
\alias{design_incomplete_block}
\title{Generate a General Incomplete Block Design (BIBD)}
\usage{
design_incomplete_block(genotypes, block_size, replicates, seed = NULL)
}
\arguments{
\item{genotypes}{Character vector of genotype names.}

\item{block_size}{Integer.  Number of plots per block (\eqn{k}).
Must be < \code{length(genotypes)}.}

\item{replicates}{Integer.  Number of times each genotype appears
across all blocks (\eqn{r}).}

\item{seed}{Integer or \code{NULL}.  Random seed for reproducibility.}
}
\value{
An S3 object of class \code{field_design} with components:
\describe{
\item{layout}{Data frame with columns \code{block}, \code{plot_id}, \code{genotype}.}
\item{design_type}{\code{"BIBD"}.}
\item{parameters}{Named list with \code{v}, \code{k}, \code{r}, \code{b}, \code{lambda}.}
\item{seed}{The seed used.}
}
}
\description{
Creates a Balanced Incomplete Block Design (BIBD) for a user-supplied
vector of genotype names.  In a BIBD every pair of treatments appears
together in the same block the same number of times (\eqn{\lambda}
times).
}
\details{
The function generates an approximate BIBD by randomising genotype
assignments to blocks while meeting the replication constraint.
Exact BIBD balance (equal \eqn{\lambda}) is achieved when
\eqn{r(k-1) = \lambda(v-1)}.
}
\examples{
genos <- paste0("G", 1:7)
d <- design_incomplete_block(genos, block_size = 3L, replicates = 3L,
                             seed = 1L)
d$layout

}
\references{
Cochran, W. G., & Cox, G. M. (1957). \emph{Experimental Designs} (2nd ed.).
Wiley.
}
\seealso{
\code{\link[=design_alpha_lattice]{design_alpha_lattice()}}, \code{\link[=design_augmented]{design_augmented()}}
}
