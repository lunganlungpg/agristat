% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phylogenetics.R
\name{write_tree}
\alias{write_tree}
\title{Export a Phylogenetic Tree to File}
\usage{
write_tree(tree, file, format = "newick", append = FALSE)
}
\arguments{
\item{tree}{A \code{phylo_tree_ml}, \code{phylo_tree_nj}, \code{bootstrap_result}, or
\code{ape::phylo} object.}

\item{file}{Character. Output file path.}

\item{format}{Character. \code{"newick"} (default) or \code{"nexus"}.}

\item{append}{Logical. Append to existing file? Default \code{FALSE}.}
}
\value{
The tree string (invisibly).
}
\description{
Writes a tree to Newick or Nexus format (compatible with FigTree, iTOL,
Mesquite).
}
\examples{
fasta_lines <- c(
  ">Seq1", "ATGCATGCATGCATGCATGC",
  ">Seq2", "ATGCTTGCATGCATGCTTGC",
  ">Seq3", "ATGCATGCTTGCATGCATGC",
  ">Seq4", "ATGCATGCATGCTTGCATGC"
)
tmp_fasta <- tempfile(fileext = ".fasta")
writeLines(fasta_lines, tmp_fasta)
seqs <- read_fasta(tmp_fasta)
aln  <- align_sequences(seqs)
tree <- build_tree_nj(aln)
tmp_nwk <- tempfile(fileext = ".nwk")
write_tree(tree, file = tmp_nwk, format = "newick")
cat(readLines(tmp_nwk), sep = "\n")

}
