% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field-layouts.R
\name{analyze_design}
\alias{analyze_design}
\title{Analyse an Experimental Design}
\usage{
analyze_design(design, response, data)
}
\arguments{
\item{design}{A \code{field_design} object (created by one of the
\verb{design_*()} functions).}

\item{response}{Character string.  Name of the response variable column
in \code{data}.}

\item{data}{A data frame containing the response variable and all design
columns (must include \code{plot_id} to join with \code{design$layout}).}
}
\value{
An S3 object of class \code{c("agristat_aov", "aov")} or
\code{c("agristat_aov", "lmerMod")} (for split-plot designs), depending on
the design type.  The object carries a \code{design} attribute with the
original \code{field_design}.
}
\description{
Dispatches to the appropriate statistical analysis based on the design type
stored in a \code{field_design} object.  Supports CRD, RCBD, factorial, Latin
Square, split-plot, split-split-plot, and strip-plot analyses.
}
\details{
\tabular{lll}{
   Design \tab Model \tab Function \cr
   CRD \tab \code{response ~ treatment} \tab \code{stats::aov()} \cr
   RCBD \tab \code{response ~ treatment + block} \tab \code{stats::aov()} \cr
   Factorial \tab \code{response ~ A * B * ...} \tab \code{stats::aov()} \cr
   Split-plot \tab mixed model \tab \code{lme4::lmer()} \cr
   Latin Square \tab \code{response ~ row + column + treatment} \tab \code{stats::aov()} \cr
}
}
\examples{
set.seed(1L)
d <- design_crd(c("A", "B", "C"), replicates = 5L, seed = 1L)
df <- d$layout
df$yield <- c(rnorm(5, 10), rnorm(5, 12), rnorm(5, 11))
result <- analyze_design(d, response = "yield", data = df)
summary(result)

}
\seealso{
\code{\link[=design_crd]{design_crd()}}, \code{\link[=design_rcbd]{design_rcbd()}}, \code{\link[=design_factorial]{design_factorial()}},
\code{\link[=plot_anova_diagnostics]{plot_anova_diagnostics()}}
}
