% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field-layouts.R
\name{design_factorial}
\alias{design_factorial}
\title{Generate a Factorial Experimental Design}
\usage{
design_factorial(factor_names, factor_levels, replicates, seed = NULL)
}
\arguments{
\item{factor_names}{Character vector of factor names (length \eqn{\geq 2}).}

\item{factor_levels}{Named list where each element is a character (or
numeric) vector of levels for the corresponding factor.  The list names
must match \code{factor_names}.}

\item{replicates}{Positive integer.  Number of replicates per cell.}

\item{seed}{Integer or \code{NULL}.  Random seed.}
}
\value{
An S3 object of class \code{c("factorial_design", "field_design")} whose
\code{layout} data frame contains columns for each factor plus \code{plot_id},
\code{treatment} (concatenated factor-level string), \code{block} (\code{NA}), \code{row},
and \code{column}.  The \code{metadata} element includes \code{contrast_coding} with
all main-effect and interaction term names.
}
\description{
Creates a complete factorial design crossing all combinations of two or
more factors.  Supports 2-way, 3-way, and higher-order factorials.  Each
unique treatment combination (cell) is replicated \code{replicates} times.
}
\details{
For a two-factor factorial with factors A (a levels) and B (b levels):
\deqn{y_{ijk} = \mu + \alpha_i + \beta_j + (\alpha\beta)_{ij} +
  \varepsilon_{ijk}}
}
\examples{
d <- design_factorial(
  factor_names  = c("Nitrogen", "Variety"),
  factor_levels = list(
    Nitrogen = c("Low", "High"),
    Variety  = c("V1", "V2", "V3")
  ),
  replicates = 3L,
  seed       = 1L
)
print(d)
nrow(d$layout)   # 2 * 3 * 3 = 18 plots

}
\seealso{
\code{\link[=design_crd]{design_crd()}}, \code{\link[=design_rcbd]{design_rcbd()}}, \code{\link[=analyze_design]{analyze_design()}}
}
