% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genetics.R
\name{design_alpha_lattice}
\alias{design_alpha_lattice}
\title{Generate an Alpha-Lattice (Resolvable Incomplete Block) Design}
\usage{
design_alpha_lattice(num_genotypes, block_size, replicates, seed = NULL)
}
\arguments{
\item{num_genotypes}{Integer.  Total number of genotypes to evaluate
(>= 4).}

\item{block_size}{Integer.  Number of plots per incomplete block.
Must divide \code{num_genotypes} evenly.}

\item{replicates}{Integer.  Number of complete replicates (>= 2).}

\item{seed}{Integer or \code{NULL}.  Random seed for reproducibility.}
}
\value{
A \code{\link[=data.frame]{data.frame()}} with columns:
\describe{
\item{rep}{Replicate number (integer).}
\item{block}{Incomplete block number within replicate (integer).}
\item{plot_id}{Sequential plot identifier across the whole trial.}
\item{genotype}{Genotype label (character, "G001", "G002", ...).}
}
}
\description{
Constructs an alpha-lattice field design suitable for large trials with
many genotypes (typically > 100).  The design is resolvable: each
replicate contains every genotype exactly once, distributed across
incomplete blocks of equal size.
}
\details{
An alpha-lattice is a resolvable Balanced Incomplete Block Design (BIBD)
where:
\itemize{
\item \eqn{v} = number of genotypes,
\item \eqn{k} = block size (must divide \eqn{v}),
\item \eqn{r} = number of replicates,
\item blocks per replicate = \eqn{v / k}.
}
}
\examples{
# 20 genotypes, block size 4, 3 replicates
d <- design_alpha_lattice(20L, 4L, 3L, seed = 42L)
head(d)
nrow(d) # 20 * 3 = 60

}
\references{
Patterson, H. D., & Williams, E. R. (1976). A new class of resolvable
incomplete block designs. \emph{Biometrika}, 63(1), 83--92.
}
\seealso{
\code{\link[=design_incomplete_block]{design_incomplete_block()}}, \code{\link[=design_augmented]{design_augmented()}}
}
