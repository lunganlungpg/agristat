% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genetics.R
\name{run_basic_gwas}
\alias{run_basic_gwas}
\title{Run a Basic GWAS (Genome-Wide Association Study)}
\usage{
run_basic_gwas(
  phenotype,
  marker_genotypes,
  marker_positions = NULL,
  trait_type = "quantitative",
  min_maf = 0.05
)
}
\arguments{
\item{phenotype}{Numeric vector (quantitative) or integer vector of 0/1
(binary) trait values.  Length must equal \code{nrow(marker_genotypes)}.}

\item{marker_genotypes}{Numeric matrix (individuals x SNPs, 0/1/2
coding).  Column names are used as marker IDs.}

\item{marker_positions}{A data frame with columns \code{marker}, \code{chromosome},
and \code{position}.  If \code{NULL}, a default layout is generated.}

\item{trait_type}{Character.  \code{"quantitative"} (default) or \code{"binary"}.}

\item{min_maf}{Numeric.  Minimum minor allele frequency filter
(default \code{0.05}).}
}
\value{
An S3 object of class \code{gwas_result} with components:
\describe{
\item{results}{Data frame: \code{marker}, \code{chromosome}, \code{position},
\code{p_value}, \code{neg_log10_p}, \code{beta}, \code{se}, \code{maf}.}
\item{trait_type}{\code{"quantitative"} or \code{"binary"}.}
\item{n_individuals}{Number of individuals analysed.}
\item{n_markers_tested}{Number of SNPs after MAF filter.}
\item{bonferroni_threshold}{Bonferroni-corrected p-value threshold.}
}
}
\description{
Performs marker-by-marker association tests between a phenotype vector
and a matrix of SNP genotypes (coded 0/1/2).  Supports linear regression
for quantitative traits and logistic regression for binary traits.
}
\details{
For each SNP \eqn{j}, the model is:
\deqn{y = \mu + \beta_j x_j + \varepsilon}
where \eqn{x_j \in \{0,1,2\}} is the allele dosage.  P-values are from
the Wald test; effect sizes are the regression coefficient
\eqn{\hat{\beta}_j}.
}
\examples{
set.seed(6)
n <- 100L; m <- 50L
pheno <- rnorm(n)
gmat  <- matrix(sample(0:2, n * m, replace = TRUE,
                        prob = c(0.25, 0.5, 0.25)),
                nrow = n,
                dimnames = list(NULL, paste0("SNP", seq_len(m))))
pos <- data.frame(marker     = paste0("SNP", seq_len(m)),
                  chromosome = rep(1:5, each = 10L),
                  position   = rep(seq(1, 100, 10), 5L))
g <- run_basic_gwas(pheno, gmat, pos)
nrow(g$results)

}
\references{
Visscher, P. M., Wray, N. R., Zhang, Q., et al. (2017). 10 years of
GWAS discovery: biology, function, and translation.
\emph{American Journal of Human Genetics}, 101(1), 5--22.
}
\seealso{
\code{\link[=plot_manhattan]{plot_manhattan()}}, \code{\link[=plot_qq_gwas]{plot_qq_gwas()}}, \code{\link[=prepare_qtl_data]{prepare_qtl_data()}}
}
