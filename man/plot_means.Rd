% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posthoc.R
\name{plot_means}
\alias{plot_means}
\title{Bar/Point Plot with Significance Letters}
\usage{
plot_means(
  x,
  type = c("bar", "point"),
  y_label = "Mean",
  x_label = "Treatment",
  title = NULL,
  show_letters = TRUE,
  error_type = c("se", "ci"),
  mse = NULL,
  n_rep = NULL,
  fill_color = "steelblue",
  ...
)
}
\arguments{
\item{x}{A \code{mean_separation_result} object (from \code{\link[=mean_separation]{mean_separation()}}) or a
data frame with at least columns \code{treatment}, \code{mean}, and optionally
\code{letters}, \code{se}.}

\item{type}{Plot type: \code{"bar"} (default) or \code{"point"}.}

\item{y_label}{Character; y-axis label. Default \code{"Mean"}.}

\item{x_label}{Character; x-axis label. Default \code{"Treatment"}.}

\item{title}{Character; plot title.}

\item{show_letters}{Logical; whether to display significance letters
(default \code{TRUE}).}

\item{error_type}{Character; type of error bar: \code{"se"} (standard error,
default) or \code{"ci"} (95\% confidence interval).}

\item{mse}{Numeric MSE used to compute SE bars when \code{x} is a
\code{mean_separation_result} and the original data are not available.}

\item{n_rep}{Integer replication count for SE calculation.}

\item{fill_color}{Character; bar fill colour (default \code{"steelblue"}).}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{ggplot2} object.
}
\description{
Creates a publication-ready ggplot2 bar chart (or point chart) of treatment
means with DMRT/Tukey significance letters and error bars.
}
\examples{
set.seed(42)
dat <- data.frame(
  y = c(rnorm(5, 10), rnorm(5, 12), rnorm(5, 14)),
  trt = factor(rep(c("A", "B", "C"), each = 5))
)
fit <- aov(y ~ trt, data = dat)
res <- mean_separation(fit, method = "dmrt")
p <- plot_means(res)
print(p)

}
