% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-ingestion.R
\name{import_field_data}
\alias{import_field_data}
\title{Import Field Trial Data}
\usage{
import_field_data(file, sheet = 1, na_strings = c("NA", "", ".", "N/A"), ...)
}
\arguments{
\item{file}{Character. Path to the data file.  Supported extensions:
\code{.csv}, \code{.tsv}, \code{.txt} (delimited text), \code{.xlsx}, \code{.xls} (Excel),
\code{.geojson}, \code{.json} (GeoJSON), \code{.shp} (Shapefile).}

\item{sheet}{Integer or character.  Sheet index or name for Excel files
(default \code{1}).}

\item{na_strings}{Character vector of strings to treat as \code{NA}
(default \code{c("NA", "", ".", "N/A")}).}

\item{...}{Additional arguments forwarded to the underlying reader
(\code{readr::read_csv()}, \code{readxl::read_excel()}, or \code{sf::st_read()}).}
}
\value{
For delimited and Excel files: a \code{\link[tibble:tibble]{tibble}} with
an \code{"agristat_metadata"} attribute (a data frame of column types).
Columns are coerced to numeric where possible; columns matching common
date patterns are parsed as \code{Date}; remaining character columns are
left as character.

For spatial files (GeoJSON, Shapefile): an \code{sf} object.
}
\description{
Reads field trial data from a variety of file formats (CSV, TSV, Excel,
GeoJSON, Shapefile) with automatic type detection, missing-value
standardisation, and optional spatial object creation.
}
\examples{
# Write a temporary CSV and import it
tmp <- tempfile(fileext = ".csv")
write.csv(
  data.frame(plot = 1:4, yield = c(3.2, 4.1, 3.8, 4.5), trt = c("A","B","A","B")),
  tmp, row.names = FALSE
)
df <- import_field_data(tmp)
print(df)
attr(df, "agristat_metadata")

}
\seealso{
\code{\link[=check_assumptions]{check_assumptions()}}, \code{\link[=declare_hypothesis]{declare_hypothesis()}}
}
