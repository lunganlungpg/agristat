% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genetics.R
\name{prepare_qtl_data}
\alias{prepare_qtl_data}
\title{Prepare Data for QTL Mapping}
\usage{
prepare_qtl_data(phenotypes, marker_genotypes, map_info = NULL, id_col = "id")
}
\arguments{
\item{phenotypes}{A data frame with at minimum one numeric column for
the trait of interest and an \code{id} column matching row names/IDs in
\code{marker_genotypes}.}

\item{marker_genotypes}{A matrix or data frame (individuals in rows,
markers in columns).  Values should be 0/1/2 or A/H/B.}

\item{map_info}{Optional data frame with columns \code{marker} and
\code{chromosome} (and optionally \code{position}).}

\item{id_col}{Character.  Name of the individual ID column in
\code{phenotypes} (default \code{"id"}).}
}
\value{
A named list with components:
\describe{
\item{phenotypes}{Aligned phenotype data frame.}
\item{genotypes}{Numeric marker matrix (0/1/2 coding).}
\item{map_info}{Map data frame (may be auto-generated).}
\item{n_individuals}{Integer.}
\item{n_markers}{Integer.}
\item{qc_report}{Data frame with per-marker QC statistics.}
}
}
\description{
Validates and reformats phenotype and marker genotype data for
downstream QTL analysis, e.g. with the \strong{qtl} package.
}
\details{
Marker coding:
\itemize{
\item Numeric (0, 1, 2): AA, Aa, aa.
\item Character (A, H, B): converted to (2, 1, 0).
}
}
\examples{
set.seed(4)
n <- 50L; m <- 10L
pheno <- data.frame(id = paste0("ind", seq_len(n)),
                    yield = rnorm(n))
markers <- matrix(sample(0:2, n * m, replace = TRUE), nrow = n,
                  dimnames = list(paste0("ind", seq_len(n)),
                                  paste0("M", seq_len(m))))
qtl_data <- prepare_qtl_data(pheno, markers)
qtl_data$n_markers

}
\seealso{
\code{\link[=plot_qtl_map]{plot_qtl_map()}}, \code{\link[=run_basic_gwas]{run_basic_gwas()}}
}
