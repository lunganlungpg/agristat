% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thesis-export.R
\name{export_results_table}
\alias{export_results_table}
\title{Export a Results Table in Various Formats}
\usage{
export_results_table(
  results_obj,
  format = "markdown",
  file = NULL,
  digits = 4L,
  stars = TRUE
)
}
\arguments{
\item{results_obj}{A result object.  Accepted classes:
\describe{
\item{\code{agristat_aov}}{ANOVA table (Source, Df, SS, MS, F, p).}
\item{\code{ammi_result}}{AMMI variance partitioning table.}
\item{\code{gwas_result}}{Top-SNP summary table.}
\item{\code{dmrt_result}, \code{tukey_result}, \code{tukey_hsd_result},
\code{dunnett_result}, \code{mean_separation_result}}{Means and letter
groupings.}
}}

\item{format}{Character.  One of \code{"markdown"} (default), \code{"html"},
\code{"latex"}, or \code{"csv"}.}

\item{file}{Character or \code{NULL} (default).  If a file path is supplied the
table is written to that file; otherwise the formatted string is returned.}

\item{digits}{Integer.  Number of significant digits for numeric columns
(default \code{4}).}

\item{stars}{Logical.  If \code{TRUE} (default) significance stars are appended
to p-value columns where applicable.}
}
\value{
A character string containing the formatted table, or \code{NULL}
(invisibly) if \code{file} is specified.
}
\description{
Converts an agristat result object into a formatted table suitable for
inclusion in a thesis, report, or publication.  Supported output formats
are Markdown, HTML, LaTeX, and CSV.
}
\examples{
set.seed(1)
dat <- data.frame(
  yield = c(rnorm(5, 4), rnorm(5, 5), rnorm(5, 3)),
  trt   = rep(c("A", "B", "C"), each = 5)
)
d   <- design_crd(c("A", "B", "C"), replicates = 5L, seed = 1L)
res <- analyze_design(d, response = "yield", data = d$layout)
cat(export_results_table(res))

}
\seealso{
\code{\link[=generate_methods_text]{generate_methods_text()}}, \code{\link[=compile_report]{compile_report()}}
}
