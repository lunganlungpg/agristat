% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genetics.R
\name{calc_heritability}
\alias{calc_heritability}
\title{Compute Broad- and Narrow-Sense Heritability}
\usage{
calc_heritability(
  variance_components,
  n_boot = 1000L,
  ci_level = 0.95,
  seed = NULL
)
}
\arguments{
\item{variance_components}{A data frame with columns \code{source} (character)
and \code{variance} (numeric).  Required sources: \code{"genotype"} and
\code{"residual"} (or \code{"error"}).  Optional: \code{"additive"}.}

\item{n_boot}{Integer.  Number of bootstrap replicates for confidence
intervals (default \code{1000L}; set to \code{0L} to skip).}

\item{ci_level}{Numeric.  Confidence level (default \code{0.95}).}

\item{seed}{Integer or \code{NULL}.  Random seed for bootstrap.}
}
\value{
An S3 object of class \code{heritability_result} with components:
\describe{
\item{H2}{Broad-sense heritability estimate.}
\item{h2}{Narrow-sense heritability estimate (\code{NA} if additive
variance not provided).}
\item{ci_H2}{Two-element numeric vector: lower and upper CI for
\eqn{H^2} (or \code{NA} if \code{n_boot = 0}).}
\item{ci_h2}{Two-element numeric vector for \eqn{h^2} (\code{NA} if not
computed).}
\item{variance_components}{The input data frame.}
\item{interpretation}{A character string with a plain-language
summary.}
}
}
\description{
Estimates broad-sense heritability \eqn{H^2} and, optionally,
narrow-sense heritability \eqn{h^2} from a data frame of variance
components obtained from a REML fit (e.g., via \code{\link[lme4:lmer]{lme4::lmer()}}).
}
\details{
\deqn{H^2 = \frac{V_G}{V_G + V_E}}
\deqn{h^2 = \frac{V_A}{V_G + V_E}}

Optional bootstrap confidence intervals are computed by resampling the
ratio statistic.
}
\examples{
vc <- data.frame(
  source   = c("genotype", "residual"),
  variance = c(2.5, 1.0)
)
h <- calc_heritability(vc, n_boot = 0L)
h$H2  # 0.714...

}
\references{
Falconer, D. S., & Mackay, T. F. C. (1996). \emph{Introduction to
Quantitative Genetics} (4th ed.). Longman.

Holland, J. B., Nyquist, W. E., & Cervantes-Martinez, C. T. (2003).
Estimating and interpreting heritability for plant breeding.
\emph{Plant Breeding Reviews}, 22, 9--112.
}
\seealso{
\code{\link[=analyze_gxe]{analyze_gxe()}}, \code{\link[=fit_ammi]{fit_ammi()}}
}
