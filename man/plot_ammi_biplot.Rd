% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genetics.R
\name{plot_ammi_biplot}
\alias{plot_ammi_biplot}
\title{AMMI Biplot}
\usage{
plot_ammi_biplot(x, pc_x = 1L, pc_y = 2L, label_size = 3, arrow_length = 1)
}
\arguments{
\item{x}{An \code{ammi_result} object returned by \code{\link[=fit_ammi]{fit_ammi()}}.}

\item{pc_x}{Integer.  IPCA axis for the x-axis (default \code{1L}).}

\item{pc_y}{Integer.  IPCA axis for the y-axis (default \code{2L}).}

\item{label_size}{Numeric.  Text label size (default \code{3}).}

\item{arrow_length}{Numeric.  Length multiplier for environment arrows
(default \code{1}).}
}
\value{
A \code{ggplot} object.
}
\description{
Produces a ggplot2 biplot of AMMI scores and loadings, showing genotype
scores and environment vectors radiating from the origin.
}
\examples{
set.seed(3)
df <- expand.grid(genotype     = paste0("G", 1:5),
                  environment  = paste0("E", 1:4),
                  stringsAsFactors = FALSE)
df$yield <- rnorm(nrow(df), 5, 1.5)
am <- fit_ammi(df, response = "yield")
p  <- plot_ammi_biplot(am)
# print(p)  # display in interactive session

}
\seealso{
\code{\link[=fit_ammi]{fit_ammi()}}
}
