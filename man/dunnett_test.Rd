% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posthoc.R
\name{dunnett_test}
\alias{dunnett_test}
\title{Dunnett's Test for Treatment vs Control Comparisons}
\usage{
dunnett_test(
  response,
  group,
  control,
  alpha = 0.05,
  alternative = c("two.sided", "less", "greater")
)
}
\arguments{
\item{response}{Numeric vector of response values.}

\item{group}{Factor or character vector indicating group membership. The
control group is identified by \code{control}.}

\item{control}{Character string naming the control level in \code{group}.}

\item{alpha}{Significance level (default \code{0.05}).}

\item{alternative}{Direction of the test: \code{"two.sided"} (default),
\code{"less"}, or \code{"greater"}.}
}
\value{
An S3 object of class \code{dunnett_result} containing:
\describe{
\item{\code{comparisons}}{Data frame with columns \code{treatment}, \code{estimate},
\code{se}, \code{t_stat}, \code{p_value} for each treatment vs control.}
\item{\code{significant}}{Named logical vector indicating significance.}
\item{\code{control}}{Name of the control group.}
\item{\code{alpha}}{Significance level used.}
\item{\code{alternative}}{Alternative hypothesis direction.}
\item{\code{interpretation}}{Character summary.}
}
}
\description{
Performs Dunnett's test comparing each treatment group against a single
control group. Unlike pairwise tests (Tukey, DMRT), Dunnett's test only
compares treatments to the control, providing greater power for this
restricted set of comparisons while controlling the family-wise error rate.
}
\details{
Dunnett's test uses a multivariate \eqn{t} critical value that accounts for
the correlation among the treatment-vs-control comparisons. The test
statistic for treatment \eqn{i} vs control is:
\deqn{t_i = \frac{\bar{y}_i - \bar{y}_0}{s_p \sqrt{1/n_i + 1/n_0}}}
where \eqn{s_p = \sqrt{MSE}} is the pooled standard deviation.

This implementation uses the \code{multcomp} package when available, falling back
to a conservative Bonferroni-adjusted \eqn{t} test otherwise.
}
\examples{
set.seed(1)
response <- c(rnorm(5, 10), rnorm(5, 13), rnorm(5, 10.5))
group <- factor(rep(c("control", "drugA", "drugB"), each = 5))
res <- dunnett_test(response, group, control = "control")
print(res)

}
\seealso{
\code{\link[=tukey_hsd]{tukey_hsd()}}, \code{\link[=dmrt]{dmrt()}}, \code{\link[=mean_separation]{mean_separation()}}
}
