% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posthoc.R
\name{mean_separation}
\alias{mean_separation}
\title{Unified Mean Separation Dispatcher}
\usage{
mean_separation(
  model,
  method = c("tukey", "dmrt", "dunnett", "contrasts"),
  alpha = 0.05,
  control_group = NULL,
  contrast_matrix = NULL,
  ...
)
}
\arguments{
\item{model}{A fitted \code{"aov"} object from \code{\link[stats:aov]{stats::aov()}}.}

\item{method}{Character string selecting the test method:
\code{"dmrt"} (Duncan's MRT), \code{"tukey"} (Tukey's HSD),
\code{"dunnett"} (Dunnett's test), or \code{"contrasts"} (orthogonal contrasts).
Default is \code{"tukey"}.}

\item{alpha}{Significance level (default \code{0.05}).}

\item{control_group}{Character string naming the control group. Required
when \code{method = "dunnett"}.}

\item{contrast_matrix}{Numeric matrix of contrast coefficients. Required
when \code{method = "contrasts"}.}

\item{...}{Additional arguments passed to the selected test function.}
}
\value{
An S3 object of class \code{mean_separation_result} containing:
\describe{
\item{\code{method_used}}{Character naming the method.}
\item{\code{results}}{The raw result object from the selected test.}
\item{\code{means_with_letters}}{Data frame with treatment, mean, and
significance letters (suitable for publication tables).}
\item{\code{recommendations}}{Character with method selection rationale.}
\item{\code{alpha}}{Significance level.}
}
}
\description{
A unified interface that routes to the appropriate post-hoc test based on
the user's choice and the ANOVA model structure. Provides a single entry
point for all mean separation methods in the agristat package.
}
\details{
\strong{Choosing a method:}\tabular{lll}{
   Method \tab Use when \tab Error rate \cr
   DMRT \tab Exploratory; max power \tab Experimentwise \cr
   Tukey \tab Confirmatory; all pairs \tab Family-wise (FWER) \cr
   Dunnett \tab Compare to control only \tab Family-wise (FWER) \cr
   Contrasts \tab Planned comparisons \tab Per-contrast \cr
}
}
\examples{
set.seed(42)
dat <- data.frame(
  y = c(rnorm(5, 10), rnorm(5, 12), rnorm(5, 14)),
  trt = factor(rep(c("A", "B", "C"), each = 5))
)
fit <- aov(y ~ trt, data = dat)

# Tukey HSD (default)
res_tukey <- mean_separation(fit, method = "tukey")
print(res_tukey)

# DMRT
res_dmrt <- mean_separation(fit, method = "dmrt")
print(res_dmrt)

}
\seealso{
\code{\link[=dmrt]{dmrt()}}, \code{\link[=tukey_hsd]{tukey_hsd()}}, \code{\link[=dunnett_test]{dunnett_test()}}, \code{\link[=test_contrasts]{test_contrasts()}},
\code{\link[=plot_means]{plot_means()}}
}
