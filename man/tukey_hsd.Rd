% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posthoc.R
\name{tukey_hsd}
\alias{tukey_hsd}
\title{Tukey's Honestly Significant Difference Test}
\usage{
tukey_hsd(model, mse = NULL, df = NULL, n = NULL, alpha = 0.05, which = NULL)
}
\arguments{
\item{model}{A fitted model of class \code{"aov"} (from \code{\link[stats:aov]{stats::aov()}}), or a
named numeric vector of treatment means (requires \code{mse} and \code{df}).}

\item{mse}{Mean squared error. Required when \code{model} is a numeric vector.}

\item{df}{Residual degrees of freedom. Required when \code{model} is a numeric
vector.}

\item{n}{Replication count(s). Required when \code{model} is a numeric vector.
Either a single integer or a named integer vector.}

\item{alpha}{Significance level (default \code{0.05}).}

\item{which}{Character vector naming the factors to compare. Passed to
\code{\link[stats:TukeyHSD]{stats::TukeyHSD()}} when \code{model} is an \code{"aov"} object.}
}
\value{
An S3 object of class \code{tukey_result} containing:
\describe{
\item{\code{comparisons}}{Data frame with columns \code{comparison}, \code{diff},
\code{lwr}, \code{upr}, \code{p_adj}.}
\item{\code{letters}}{Named character vector of significance letters.}
\item{\code{alpha}}{Significance level used.}
\item{\code{interpretation}}{Character summary of the test.}
}
}
\description{
Performs Tukey's HSD test for all pairwise comparisons of treatment means
following a significant ANOVA. This is a wrapper around \code{\link[stats:TukeyHSD]{stats::TukeyHSD()}}
with enhanced output including significance letter assignment.
}
\details{
The Tukey HSD critical value is:
\deqn{HSD = q_{\alpha, k, \nu} \sqrt{\frac{MSE}{n}}}
where \eqn{q_{\alpha, k, \nu}} is the studentised range statistic,
\eqn{k} is the number of treatments, and \eqn{\nu} is the residual df.

Tukey's HSD controls the family-wise error rate (FWER) at \eqn{\alpha} for
all \eqn{\binom{k}{2}} pairwise comparisons simultaneously, making it more
conservative than DMRT but less conservative than Bonferroni.
}
\examples{
set.seed(42)
dat <- data.frame(
  y = c(rnorm(5, 10), rnorm(5, 12), rnorm(5, 14)),
  trt = factor(rep(c("A", "B", "C"), each = 5))
)
fit <- aov(y ~ trt, data = dat)
res <- tukey_hsd(fit)
print(res)

}
\seealso{
\code{\link[=dmrt]{dmrt()}}, \code{\link[=dunnett_test]{dunnett_test()}}, \code{\link[=mean_separation]{mean_separation()}}
}
