% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posthoc.R
\name{dmrt}
\alias{dmrt}
\title{Duncan's Multiple Range Test}
\usage{
dmrt(means, mse, df, alpha = 0.05, n = 1)
}
\arguments{
\item{means}{A named numeric vector of treatment means.}

\item{mse}{Mean squared error (MSE) from the ANOVA error term.}

\item{df}{Residual degrees of freedom from the ANOVA.}

\item{alpha}{Significance level (default \code{0.05}).}

\item{n}{Either a single integer (balanced replication) or a named integer
vector of replication counts per treatment (unequal replication). Defaults
to \code{1} (each mean is a single observation; set this correctly).}
}
\value{
An S3 object of class \code{dmrt_result} containing:
\describe{
\item{\code{means_table}}{Data frame with columns \code{treatment}, \code{mean},
\code{letters}, ordered from largest to smallest mean.}
\item{\code{critical_ranges}}{Named numeric vector of critical range values
\eqn{R_p} for \eqn{p = 2, 3, \ldots, k}.}
\item{\code{mse}}{The MSE used in the test.}
\item{\code{df}}{Residual degrees of freedom.}
\item{\code{alpha}}{Significance level used.}
\item{\code{interpretation}}{Character string summarising the groupings.}
}
}
\description{
Performs Duncan's Multiple Range Test (DMRT) for mean separation following
a significant ANOVA. DMRT controls the experimentwise error rate at
\eqn{\alpha} for each ordered range of means, allowing more comparisons to
be significant than Tukey's HSD.
}
\details{
The critical range for a span of \eqn{p} means is:
\deqn{R_p = r_\alpha(p, \nu) \times s_{\bar{y}}}
where \eqn{r_\alpha(p, \nu)} is the Duncan significant studentised range,
approximated here via the Student's \eqn{t} distribution as:
\deqn{r_\alpha(p, \nu) \approx \sqrt{2} \cdot t_{\alpha_p, \nu}}
with \eqn{\alpha_p = 1 - (1 - \alpha)^{p-1}}, and
\deqn{s_{\bar{y}} = \sqrt{\frac{MSE}{n_h}}}
with \eqn{n_h} being the harmonic mean of replications when replication is
unequal.

\strong{Decision guideline:} Use DMRT when the experimental objective is maximum
power to detect treatment differences and the experiment is exploratory.
Prefer Tukey's HSD when controlling the family-wise error rate is important
(e.g., confirmatory studies). Dunnett's test is preferred when comparisons
are only against a control.
}
\examples{
means <- c(A = 12.5, B = 10.2, C = 14.8, D = 11.0)
result <- dmrt(means, mse = 2.1, df = 20, alpha = 0.05, n = 5)
print(result)
summary(result)

# Unequal replication
reps <- c(A = 5, B = 4, C = 6, D = 5)
result2 <- dmrt(means, mse = 2.1, df = 17, alpha = 0.05, n = reps)
print(result2)

}
\references{
Duncan, D.B. (1955). Multiple range and multiple F tests.
\emph{Biometrics}, \strong{11}, 1–42. \doi{10.2307/3001478}
}
\seealso{
\code{\link[=tukey_hsd]{tukey_hsd()}}, \code{\link[=dunnett_test]{dunnett_test()}}, \code{\link[=mean_separation]{mean_separation()}}
}
