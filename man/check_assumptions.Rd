% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-ingestion.R
\name{check_assumptions}
\alias{check_assumptions}
\title{Check Statistical Assumptions}
\usage{
check_assumptions(x, groups = NULL, alpha = 0.05)
}
\arguments{
\item{x}{A numeric vector (NAs are silently removed before testing).}

\item{groups}{An optional factor or character vector of the same length as
\code{x} identifying group membership.  Required for Levene's test.}

\item{alpha}{Numeric significance level (default \code{0.05}).}
}
\value{
An S3 object of class \code{"assumption_report"} with components:
\describe{
\item{\code{test_results}}{Data frame with columns \code{test_name},
\code{statistic}, \code{p_value}, \code{passed_at_alpha}.}
\item{\code{interpretation}}{Named character vector — \code{"PASS"} or \code{"FAIL"}
for each test.}
\item{\code{recommendation}}{Character string with a plain-language
suggestion (e.g., apply a transformation or use a non-parametric
test).}
\item{\code{alpha}}{The significance level used.}
\item{\code{n_groups}}{Integer.  Number of distinct groups (or \code{NA} if no
grouping variable was supplied).}
}
}
\description{
Runs a battery of assumption tests on a numeric vector.  By default a
Shapiro-Wilk normality test is performed.  When a grouping variable is
supplied, Levene's test for homogeneity of variance is added.  The result
is an S3 object of class \code{"assumption_report"} containing test statistics,
pass/fail flags, and a plain-language recommendation.
}
\examples{
set.seed(42)
x <- rnorm(30, mean = 5, sd = 1)
report <- check_assumptions(x)
print(report)

# With grouping variable (adds Levene's test)
grp <- rep(c("A", "B", "C"), each = 10)
report2 <- check_assumptions(x, groups = grp)
summary(report2)

}
\seealso{
\code{\link[=auto_transform]{auto_transform()}}, \code{\link[=suggest_nonparametric]{suggest_nonparametric()}}
}
