# Raw data scripts
#
# This directory contains scripts used to prepare datasets bundled with
# the agristat package.  Processed datasets are stored in `data/` as
# `.rda` files.
#
# Scripts follow the pattern: `data-raw/<dataset-name>.R`
